# `context.md` template

The standard briefing every reviewer reads. Generated by `context-bundle-write` from the other primitives' outputs. Mirrors `sui-pilot:move-pr-review`'s `context_bundle_template.md` sections 1–7 + 11–12, with the leads/shortlist section gated behind `--anti-bias`.

Placeholders are filled by the composing script. All sections are required except where marked optional.

---

# {PR_TITLE_OR_BRANCH} — Shared Context Bundle

> Read this **completely** before reviewing. Single source of truth shared with all other reviewers. The consolidator uses it to normalize findings.

## 1. Target

{TARGET_BLOCK}
<!-- For PR target:
- **Repo:** {OWNER/REPO}
- **PR:** #{NUMBER} — {TITLE}
- **Base:** {BASE_REF}  **Head:** {HEAD_REF}
- **HEAD commit:** `{HEAD_OID}`
- **Diff size:** +{ADDITIONS} / −{DELETIONS} across {FILE_COUNT} files
- **Single commit?** {YES|NO} — if multi-commit, see commits below
-->

<!-- For branch target:
- **Branch:** {HEAD_REF} (vs `{BASE_REF}`)
- **HEAD commit:** `{HEAD_OID}`
- **Diff size:** +{ADDITIONS} / −{DELETIONS} across {FILE_COUNT} files
-->

<!-- For worktree target:
- **Mode:** working tree vs HEAD
- **HEAD commit:** `{HEAD_OID}`
- **Diff size:** +{ADDITIONS} / −{DELETIONS}
-->

## 2. Review scope — IN

Every file listed must be audited at least once.

{SCOPE_FILES_BLOCK}
<!-- bulleted list, one per file in scope_files.txt -->

## 3. Review scope — OUT (read for context only)

{SCOPE_OUT_BLOCK}
<!-- usually "(none)" unless orchestrator passed an out-of-scope list -->

## 4. Dep pins (capture for reproducibility)

{DEP_PINS_BLOCK}
<!-- For each entry in dep-pins.json:
- `{NAME}` (in `{MANIFEST}`) pinned to `{REV}` — {⚠️ BRANCH PIN if is_branch}
  - Local clone: `{LOCAL_CLONE_PATH}` HEAD `{LOCAL_HEAD}` — *"{LOCAL_HEAD_SUBJECT}"*
  - Or: "no local clone found"
If dep-pins.json is empty: "(no git deps)" -->

## 5. Repo conventions

Reviewers MUST honor every CLAUDE.md listed at:

`{CLAUDE_MD_PATHS_FILE}`

{CLAUDE_MD_PREVIEW}
<!-- bulleted list of paths (max 10) for at-a-glance reference -->

## 6. Design intent

{DESIGN_DOCS_BLOCK}
<!-- if design-docs/index.md is non-empty: "See `{out}/design-docs/index.md`."
     if empty: "(no Linear/Notion docs referenced in PR body)" -->

## 7. Finding schema and severity rubric (STRICT)

All findings must conform to the schema and severity rubric documented at:

`{SCHEMAS_PATH}`

Quick reference:
- `severity` ∈ `critical | high | medium | low | info`
- `category` — one of 23 (see schema doc)
- `evidence` is a literal quote, ≥ 1 full line, no ellipsis
- `spec_reference` is REQUIRED for critical/high findings

## 8. Working directory and prohibitions

- **cwd:** `{REPO_ROOT}`
- **NO** edits to source / manifests / anything outside your reviewer artifact file
- **NO** `sui move build`, `forge`, `pnpm install`, `git commit`, `git push`, mutating `gh` commands
- **NO** test runs unless your reviewer prompt explicitly authorizes them

## 9. Budget

- Target ~30–45 minutes per reviewer.
- Target 10–30 findings. Quality > quantity.
- Emit what you have if you run out of budget.

{LEADS_SECTION_OR_EMPTY}
<!-- when --anti-bias: this block is empty.
     otherwise: include section 10 below
-->

<!-- ## 10. LEADS — confirm, refute, or ignore (DO NOT TRUST)
Things the orchestrator noticed during pre-read. **NOT** findings — confirm with independent evidence, refute, or ignore.

1. <one-line lead>
2. <one-line lead>
-->
