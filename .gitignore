# ── Runtime & session data (machine-specific, ephemeral) ──
.last-update-result.json
security/
backups/
cache/
debug/
downloads/
file-history/
history.jsonl
ide/
mcp-needs-auth-cache.json
paste-cache/
plans/
projects/
security_warnings_state_*.json
session-env/
sessions/
shell-snapshots/
stats-cache.json
statsig/
tasks/
teams/
telemetry/
todos/

# ── Plugin runtime (keep config, ignore downloaded cache/repos) ──
plugins/cache/
plugins/data/
plugins/marketplaces/
plugins/known_marketplaces.json
plugins/install-counts-cache.json
plugins/plugin-catalog-cache.json
plugins/blocklist.json
plugins/.last_inuse_sweep

# ── Channels (bot tokens, access control — NEVER commit) ──
channels/

# ── OAuth / API credentials (NEVER commit) ──
.credentials.json

# ── Settings backups (auto-generated) ──
settings.json.backup.*

# ── Cleanup hook marker (regenerated each run) ──
.last-cleanup

# ── Background daemon + job state (recreated by the harness each run) ──
daemon/
daemon.lock
daemon.log
daemon.status.json
daemon-auth-*
jobs/
gh-pr-status-cache.json*
remote-settings.json

# ── sui-pilot always-on context (machine-local symlink into the installed plugin) ──
# Recreated each session by hooks/refresh-sui-pilot-symlink.sh; was a submodule pre-marketplace.
/sui-pilot

# ── EVM/Solidity-security auditor skill repos (regenerable clones) ──
# Cloned by scripts/setup-sol-sec-stack.sh; they're independent git repos,
# re-fetched on demand — never commit them into dotclaude.
skills-src/

# ── OS junk ──
.DS_Store
__pycache__/
worktrees/
