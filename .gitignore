# ── Runtime & session data (machine-specific, ephemeral) ──
backups/
cache/
debug/
downloads/
file-history/
history.jsonl
ide/
mcp-needs-auth-cache.json
paste-cache/
plans/
projects/
security_warnings_state_*.json
session-env/
sessions/
shell-snapshots/
stats-cache.json
statsig/
tasks/
telemetry/
todos/

# ── Plugin runtime (keep config, ignore downloaded cache/repos) ──
plugins/cache/
plugins/marketplaces/
plugins/known_marketplaces.json
plugins/install-counts-cache.json

# ── Channels (bot tokens, access control — NEVER commit) ──
channels/

# ── Settings backups (auto-generated) ──
settings.json.backup.*

# ── Cleanup hook marker (regenerated each run) ──
.last-cleanup

# ── Background daemon + job state (recreated by the harness each run) ──
daemon/
daemon.lock
daemon.log
daemon.status.json
jobs/
gh-pr-status-cache.json

# ── OS junk ──
.DS_Store
